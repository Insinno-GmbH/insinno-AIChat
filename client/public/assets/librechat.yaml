# LibreChat Konfigurationsversion (Aktuellste Version)
version: "1.3.13"

# Globale Einstellungen für das Insinno-Branding
interface:
  privacyPolicy:
    externalUrl: "https://insinno.de"
    openInNewTab: true
  termsOfService:
    externalUrl: "https://insinno.de"
    openInNewTab: true
  mcpServers:
    use: true

# Kundenspezifische Benutzeroberfläche im Insinno-Stil
customation:
  appName: "insinno iCore AI"
  appTitle: "insinno AI Portal"
  # Offizielle Insinno Farbpalette (Orange-Akzente auf dunklem/hellem Grund)
  brandColors:
    primary: "#FF6B00"       # Das typische insinno-Orange
    secondary: "#000000"     # Klassisch Schwarz für Schriftzüge/Header
  # Optionale Links für das Dashboard/Hilfe-Menü
  helpLink: "https://insinno.de"

# Beispiel-Endpunkte (Passe diese an deine genutzten LLMs an)
endpoints:
  custom:
    - name: "insinno iCore Engine"
      apiKey: "${OPENAI_API_KEY}"
      baseURL: "https://openai.com"
      models:
        default: ["gpt-4o", "gpt-4o-mini", "gpt-3.5-turbo"]
        fetch: false
      titleConvo: true
      summarize: true
      # Icon-Zuweisung passend zum Unternehmens-Design
      iconURL: "https://insinno.de"

# Datei-Management-Einstellungen (Verhindert Fehler bei Uploads)
fileConfig:
  endpoints:
    default:
      fileLimit: 10
      fileSizeLimit: 52428800 # 50 MB
      
mcpServers:
  confluenceServer:
    type: "streamable-http" # oder "sse" / "websocket"
    url: "https://atlassian.com"
    timeout: 60000
